<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
<p><%= link_to 'create new task', new_path, class: "btn btn-primary" %></p>
